{
  "name": "networking-microservices",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "// ---- DEVELOPMENT ----": "",
    "// dev": "Levanta todos los servicios en modo development con nodemon",
    "dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "// dev:identity": "Levanta identity-service + mongo en modo development",
    "dev:identity": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build identity-service mongo",
    "// dev:passwords": "Levanta passwords + identity + mongo en modo development",
    "dev:passwords": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build passwords-service identity-service mongo",
    "// dev:gateway": "Levanta solo el gateway en modo development",
    "dev:gateway": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build api-gateway",
    "// dev:python": "Levanta python-service + gateway en modo development",
    "dev:python": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build python-service api-gateway",
    "// seed": "BORRA todos los registros (users/sessions/areas/passwords) y siembra 2 super_admin + Redes/Gerencia (supervisor + 3 operadores c/u), cada quien con su propia contraseña de acceso (pass<Grupo><NN>). Requiere el stack corriendo (npm run dev)",
    "seed": "docker compose -f docker-compose.yml -f docker-compose.dev.yml exec identity-service node src/seeds/seed.js"
  }
}
