services:
  mongodb:
    image: mongo:latest
    container_name: networking-dev-db
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: networking-dev
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data: